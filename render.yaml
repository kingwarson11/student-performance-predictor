services:
  - type: web
    name: student-performance-predictor
    runtime: python
    buildCommand: pip install -r requirements.txt && python backend/train.py
    startCommand: gunicorn --chdir backend app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
